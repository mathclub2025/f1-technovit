import React from "react";
import { DashboardShell } from "@/components/layout/dashboard-shell";

export default function StandingsPage() {
  return (
    <DashboardShell>
      <div className="flex h-full items-center justify-center text-muted-foreground font-mono text-sm p-12 text-center">
        {/* Page deliberately left blank per user request */}
      </div>
    </DashboardShell>
  );
}
